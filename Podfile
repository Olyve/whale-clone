# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'whale-clone' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for whale-clone
  pod 'Alamofire'
  pod 'Bond'
  pod 'Moya'
  pod 'SwiftyJSON'

  target 'whale-cloneTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble'
    pod 'Quick'
  end

end
